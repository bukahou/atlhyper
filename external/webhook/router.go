// 📄 external/webhook/router.go

package webhook

// 未来支持其他来源（例如 GitHub）可在此引入

// RegisterWebhookRoutes 注册所有 webhook 子模块的路由
// func RegisterWebhookRoutes(rg *gin.RouterGroup) {
// 	// 子路由组：/webhook/dockerhub
// 	dockerHub.RegisterDockerHubWebhook(rg.Group("/dockerhub"))

// 	// 可拓展更多子模块：
// 	// github.RegisterGitHubWebhook(rg.Group("/github"))
// }
