// atlhyper_metrics/config/defaults.go
// 默认值定义
// 所有可用的环境变量都在此处列出，便于快速查阅
package config

// ============================================================
// 布尔类型默认值
// ============================================================
var defaultBools = map[string]bool{
	"PUSH_ENABLE": true, // 是否启用主动上报指标数据
}

// ============================================================
// 字符串类型默认值
// ============================================================
var defaultStrings = map[string]string{
	// -------------------- 推送目标 --------------------
	"PUSH_URL":   "", // Agent 接收端完整 URL（若为空则根据下方参数自动推导）
	"PUSH_TOKEN": "", // Bearer Token 认证令牌（敏感信息）

	// -------------------- 服务发现（用于自动推导 URL）--------------------
	"AGENT_SVC_NAME":      "127.0.0.1",                   // Agent 服务名称或 IP
	"AGENT_SVC_NAMESPACE": "",                            // Agent 所在 K8s 命名空间（空则使用短名）
	"INGEST_PATH":         "/ingest/metrics/v1/snapshot", // 指标接收接口路径

	// -------------------- 指标采集 --------------------
	"NODE_NAME": "", // 节点名称，用于标识上报来源（空则自动获取 hostname）
	"PROC_ROOT": "/proc", // /proc 挂载路径（容器内采集宿主机指标时需挂载宿主机 /proc）
	"SYS_ROOT":  "/sys",  // /sys 挂载路径（容器内采集宿主机温度时需挂载宿主机 /sys）
}

// ============================================================
// 整数类型默认值
// ============================================================
var defaultInts = map[string]int{
	"AGENT_SVC_PORT": 8082, // Agent 服务端口
}

// ============================================================
// 时间类型默认值
// ============================================================
var defaultDurations = map[string]string{
	"PUSH_INTERVAL": "5s", // 指标上报间隔
	"PUSH_TIMEOUT":  "5s", // HTTP 请求超时时间
}
