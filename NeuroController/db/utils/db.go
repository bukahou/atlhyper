package utils

import "database/sql"

// ============================================================
// 📦 utils/db.go
//
// ✅ 功能说明：
//     提供全局共享的 SQLite 数据库连接对象，供所有模块调用。
//     避免重复打开连接，确保统一的连接管理。
//
// 📌 使用方式：
//     - 在项目启动时，由 sqlite.InitDB() 初始化 DB。
//     - 其他模块直接通过 utils.DB 使用该连接对象。
// ============================================================

// DBPath 是 SQLite 数据库存储路径（相对路径）
// 可根据实际需要调整为从环境变量读取以增强灵活性
const DBPath = "db/data/neuro.db"

// DB 是全局共享的数据库连接对象
// 在 sqlite.InitDB() 中完成初始化赋值
var DB *sql.DB
