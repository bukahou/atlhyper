# =======================================================================================
# 📄 config.yaml
#
# ✨ 配置说明：
#     控制器项目的主配置文件，定义模块启用状态、告警参数、缩容阈值等。
#     所有模块配置将通过 config.go 读取并共享。
#
# 📦 配置项说明：
# modules:
#   watcher: true                # 是否启用 watcher 模块
#   webhook: true                # 是否启用 webhook 模块
#   neuroai: false               # 是否启用 AI 模块
#
# email:
#   smtp_server: smtp.example.com
#   sender: neurobot@example.com
#   password: your-password
#   to: admin@example.com
#
# thresholds:
#   pod_crash_repeat: 3          # Pod 异常重启达到多少次触发策略
#
# ✍️ 作者：武夏锋（@ZGMF-X10A）
# 📅 创建时间：2025-06
# =======================================================================================

modules:
  watcher: true
  webhook: true
  neuroai: false

email:
  smtp_server: smtp.example.com
  sender: neurobot@example.com
  password: your-password
  to: admin@example.com

thresholds:
  pod_crash_repeat: 3
