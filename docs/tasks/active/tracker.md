# 任务追踪

> **本文件是任务状态的唯一权威源。**
> 只保留「待办」和「进行中」的任务。完成后归档到 `docs/tasks/archive/`。
>
> 状态标记：`✅` 完成 / `🔄` 进行中 / 无标记 = 待办

---

## AI Provider 激活机制优化

- 新增 Provider 默认激活，去掉全局开关（`ai_active_config.enabled`）概念
- 角色分配即生效：有角色就使用，无角色就不使用，与"激活"无关
- 清理前端设置页面中全局激活开关相关的 UI
